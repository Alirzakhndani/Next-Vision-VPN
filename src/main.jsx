import React from "react";
import { createRoot } from "react-dom/client";
import NextVisionVPN from "../NextVisionVPN.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NextVisionVPN />
  </React.StrictMode>,
);
